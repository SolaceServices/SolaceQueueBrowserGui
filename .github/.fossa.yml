version: 3

project:
  locator: solaceservices_SolaceQueueBrowserGui
  id: solaceservices_SolaceQueueBrowserGui
  name: SolaceQueueBrowserGui
  teams: []
  labels:
    - java

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
